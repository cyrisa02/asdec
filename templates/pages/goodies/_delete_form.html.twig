<form method="post" action="{{ path('app_goodies_delete', {'id': goody.id}) }}" onsubmit="return confirm('Etes-vous sûr de supprimer cet article?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ goody.id) }}">
	<button class="btn btn-danger btn-lg mt-4">Supprimer</button>

</form>
