{% extends 'base.html.twig' %}

{% block title %}Presence1 index{% endblock %}

{% block body %}
    <h1>Presence1 index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>IsPresent</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for presence1 in presence1s %}
            <tr>
                <td>{{ presence1.id }}</td>
                <td>{{ presence1.isPresent ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('app_presence1_show', {'id': presence1.id}) }}">show</a>
                    <a href="{{ path('app_presence1_edit', {'id': presence1.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_presence1_new') }}">Create new</a>
{% endblock %}
