{% extends 'base.html.twig' %}

{% block title %}Groupe
{% endblock %}

{% block body %}
	<div class="mt-4 container">
		<h1>Groupe</h1>

		<table class="table">
			<tbody>

				<tr>
					<th>Nom</th>
					<td>{{ category.name }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('app_category_index') }}" class="mt-4 ">Retour à la liste</a>

		<a href="{{ path('app_category_edit', {'id': category.id}) }}" class="mt-4 ms-4">Modifier</a>

		{{ include('pages/category/_delete_form.html.twig') }}</div>

{% endblock %}
