<form method="post" action="{{ path('app_sport_user_delete', {'id': sport_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sport_user.id) }}">
    <button class="btn">Delete</button>
</form>
